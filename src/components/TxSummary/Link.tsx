import React, {FC} from 'react';
import styled from 'styled-components';

const Link = styled.a`
    font-weight: 600;
    color: #2419ff;
    cursor: pointer;

    :hover {
        text-decoration: underline;
    }
`;

export default Link;
