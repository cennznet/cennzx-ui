if (typeof window !== 'undefined') {
    window.config = {
        // ENDPOINT: 'ws://127.0.0.1:9944/',
        ENDPOINT: 'wss://nikau.centrality.me/public/ws',
        ENV: 'local',
        FEE_BUFFER: 0.05,
        MAX_FEE_BUFFER: 0.5,
        MIN_FEE_BUFFER: 0.01
    }
}
